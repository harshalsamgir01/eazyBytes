spring:
  application:
    name: accounts
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:accounts
    driverClassName: org.h2.Driver
    username: sa
    password:


server:
  port: 8081

env:
  active: Default